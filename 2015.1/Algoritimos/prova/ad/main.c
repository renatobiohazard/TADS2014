#include <stdio.h>
#include <stdlib.h>

int main()
{
    char * resizeToFit(char * s)
    {
         int num=0,i=0;
          while(i<10){
    num=num+1;
    nome++;
    i++;
    }
        s2  =(char *)   calloc(num, sizeof(char));
        free(s);
        return s2;
    }
    return 0;
}
